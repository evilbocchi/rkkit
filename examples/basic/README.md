# Basic Example

This is a basic example of how to use `rkkit` with a `rokit.toml` file.

## Setup

1.  Navigate to this directory: `cd examples/basic`
2.  Run `bun run index.ts` to see it in action.
